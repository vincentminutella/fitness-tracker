'use client';
import { inter } from '@/app/ui/fonts';

export default function TrendUpLogoDash() {
  return (
    <div className={`${inter.className} flex leading-none text-white`}>
      <p className="text-5xl font-bold">TrendUp</p>
    </div>
  );
}
